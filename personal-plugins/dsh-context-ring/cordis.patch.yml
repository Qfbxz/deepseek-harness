# dsh-context-ring bundle patch. Config authority is the host settings
# service (namespace `context-ring`); no config block lives here any more —
# a YAML-typed config block once crashed boot when values were quoted.
- insert:
    - id: dsh-context-ring
      name: dsh-context-ring
